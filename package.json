{
  "name": "tool-hub",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "concurrently -k \"npm:dev:renderer\" \"npm:dev:electron\"",
    "dev:renderer": "vite --port 5173 --strictPort",
    "dev:electron": "cross-env ELECTRON_START_URL=http://localhost:5173 wait-on tcp:5173 && electron .",
    "build": "vite build",
    "dist:win": "electron-builder --win nsis portable",
    "dist:mac": "electron-builder --mac dmg zip",
    "dist:linux": "electron-builder --linux AppImage deb",
    "dist:all:local": "npm run build && npm run dist:win && npm run dist:mac && npm run dist:linux",
    "dist": "npm run build && electron-builder"
  },
  "build": {
    "appId": "cn.sajuna.toolhub",
    "productName": "工具合集",
    "directories": { "output": "release" },
    "files": [
      "dist/**/*",
      "electron/**/*",
      "package.json"
    ],
    "asar": true,
    "extraMetadata": { "main": "electron/main.cjs" },
    "win": {
      "target": ["nsis", "portable"],
      "icon": "public/icon.ico",
      "artifactName": "${productName} ${version} ${os}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "shortcutName": "工具合集",
      "createDesktopShortcut": false,
      "createStartMenuShortcut": false,
      "perMachine": false,
      "artifactName": "${productName} Setup ${version}.${ext}"
    },
    "mac": {
      "target": ["dmg", "zip"],
      "category": "public.app-category.utilities",
      "icon": "public/icon.icns",
      "artifactName": "${productName} ${version} ${os}.${ext}"
    },
    "linux": {
      "target": ["AppImage", "deb"],
      "category": "Utility",
      "icon": "public/icon.png",
      "artifactName": "${productName} ${version} ${os}.${ext}"
    }
  },
  "dependencies": {
    "lucide-react": "^0.541.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.21",
    "class-variance-authority": "^0.7.1",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "electron": "^28.3.3",
    "electron-builder": "^24.13.3",
    "postcss": "^8.5.6",
    "tailwind-merge": "^3.3.1",
    "tailwindcss": "^3.4.3",
    "tailwindcss-animate": "^1.0.7",
    "typescript": "^5.4.5",
    "vite": "^4.5.14",
    "wait-on": "^7.2.0"
  }
}
